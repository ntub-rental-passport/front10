# 分支 28 選定提交整合紀錄

基準：本機 main `1ec4263`。來源：origin/28 `543d50e`。
僅整合指定的四筆提交，未合併 28 分支其他歷史。

| 來源提交 | 處理結果 |
| --- | --- |
| `e576412` | 恢復 main 已移除的房東訂閱頁，保留結帳成功提示與表單清除。 |
| `590a12e` | main 已無原提交處理的衝突標記，保留目前套件、API/OCR 代理與 TypeScript 設定；以空提交記錄處理。未加入前端 GEMINI_API_KEY 注入，也未加入不存在的舊版型排除路徑。 |
| `e060f42` | 舊分支衝突清理會刪除 main 的驗證、監控、房東管理、合約與垃圾車功能，並引入錯誤 format 指令。其帳號識別及監控資料型別已由 main 涵蓋，因此保留 main，以空提交記錄處理。 |
| `543d50e` | 套用付款欄位驗證與樣式。main 的記事 API 已使用 DateValue 別名修正日期型別，保留現有身分驗證、空值與欄位驗證。 |

## 整合補正

- 加入 `/landlord/subscription` 路由及房東側欄入口，沿用房東角色保護。
- 年繳付款摘要及確認按鈕顯示完整年度金額。
- 卡號要求 16 位數字，安全碼輸入允許原驗證支援的 3–4 碼。
- 修正來源頁面在 `@apply` 中混用 `!important` 導致的 Tailwind 編譯錯誤。
- 加入訂閱頁測試：有效期限月份、過期與錯誤格式、空白姓名、非數字卡號、安全碼、同意條款、月繳／年繳金額、完成後清除資料及切回 Free。

## 限制

訂閱付款仍為來源分支的前端示範，未接金流，不會扣款，也不會將方案保存到後端。不得將前端測試通過解讀為真實付款整合完成。

## 驗證結果

- 既有 49 個前端測試檔、652 項測試通過；新增訂閱測試檔 9 項在修正測試 SSR context 後另行執行全部通過。
- `npm run build` 通過 TypeScript 與 Vite 正式建置；仍有大於 500 kB 的 bundle 提醒。
- 還原使用者原有修改後，後端 unittest 99 項全部通過（使用測試資料庫）。
- FastAPI 主程式匯入、health endpoint 與記事 API 未登入拒絕檢查通過；未連線操作正式資料庫。
- 原有 5 個本機修改檔案逐一與 stash 比對一致（忽略 CRLF/LF 差異）。
- 未進行瀏覽器手動操作或真實金流驗證。

## 本機資料保留

整合前 main 已備份至 `backup/main-before-28-20260923`。原有 `.env.example`、`backend/law_corpus.py`、`backend/llm_provider.py` 與兩個未追蹤檔案已另存 Git stash，整合完成後回復至工作目錄，不納入此次提交。
